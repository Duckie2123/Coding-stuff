[project]
name = "coding-stuff-ig"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Duckie", email = "stylianouandreas69@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
coding-stuff-ig = "coding_stuff_ig:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
